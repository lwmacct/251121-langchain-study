[project]
name = "03-simple-langgraph-tui"
version = "0.1.0"
description = "TUI tool calling demo using LangGraph and shared tools library"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["m_tools"]

[tool.uv.sources]
m_tools = { workspace = true }
