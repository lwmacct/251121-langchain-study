[project]
name = "05-chainlit-tool-call"
version = "0.1.0"
description = "Chainlit tool calling demo using LangGraph and shared tools library"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "tools",
]

[tool.uv.sources]
tools = { workspace = true }
